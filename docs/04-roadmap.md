# 04. ロードマップと未決事項

## 済み

### ローカルサーバー（`device/server/`）
- [x] Hono による `/api/chat`（SSE） `/api/stt` `/api/config` `/api/tts` `/api/health`
- [x] Claude / Gemini の呼び出しと SSE の正規化（`../aichat/worker` から移設）
- [x] 音声認識。Workers AI をやめ、**既定は macOS の SpeechAnalyzer（`ohr`）**。
      whisper とクラウド（Gemini / OpenAI）にも `/admin` から切り替えられる
- [x] 管理UI（パスワード + Cookie セッション）。設定は `data/config.json` に保存
- [x] 鍵を OS の鍵束から読めるように（`keychain:サービス/項目`）
- [x] 2モード（`stub` / `live`）
- [x] テスト 81 件（鍵の扱い・逐次配信・**切断で上流も止まる**・設定の保存・ウェイクワード・チャットの保存）

### 画面（`device/web/`）
- [x] 録音（AudioWorklet・16kHz mono WAV・プリロール付きの無音検出）
- [x] SSE の解析、文分割、読み上げのキュー
- [x] 状態表示・出典・時計・「やめる」
- [x] テスト 27 件

### 音声をサーバーに流す構成へ（`ws/`）
- [x] WebSocket `/ws`。音声のフレームを受け、状態・文字・読み上げの WAV を返す
- [x] **ウェイクワード**（連続 ASR + 文字列一致、既定「ずんだもん」）。
      **専用モデルの学習は要らなかった**（[06](06-device-implementation.md) の実測）
- [x] 状態機械をサーバーへ移した（`ws/session.ts`）。リングバッファで語頭を遡る
- [x] 実マイクで測る試験画面（`/wake.html` → `/ws?mode=wake`）。**AI を呼ばないので無料**
- [x] ブラウザからもウェイクワードで起こせるようにした

### チャット（[07](07-chat-design.md)）
- [x] `data/chats/` に1チャット1ファイルで保存。一覧・削除・件数の上限
- [x] `/api/chats` 一式と、`/api/chat` の `chatId`
- [x] 追い質問の窓・終了語・往復と時間の上限（`/admin` から変えられる）

### やめたこと
- [x] **Cloudflare Worker を廃止**。ローカルサーバーが鍵を持てる以上、
      中継を二重に置く理由が無かった（[03](03-tech-stack.md) の「サーバーは1つ」）
- [x] **「Pi へ移す」構成をやめた。** サーバーは Mac に据え置き、
      端末は繋ぐだけにする（下の「変更の記録」）

## これから

### Phase A: Mac で一周を確かめる — **通った**
- [x] VOICEVOX を Docker で起動し、`/api/tts` で実際に喋らせる
      （0.25.2 / `cpu-arm64-latest`。実時間比 0.4〜0.8x、最初の声まで 1.94 秒）
- [ ] 話者を決める（いまは 3 = ずんだもん ノーマル）
- [x] マイクで話しかけて回答が読み上げられるところまで通した（live / Gemini）
- [x] 読み上げの速さを `/admin` から変えられるようにした（既定 150%）
- [x] 途切れの解消: 合成と再生を分けて先読み／文末でだけ切る
- [ ] `STUB_SAVE_AUDIO=1` で保存した WAV を聴き、語頭が切れていないか確かめる
- [x] 読み上げが文ごとに途切れる問題を解消（合成と再生を分けて先読み）
- [ ] `?scenario=` でエラー・遅延・空応答の見え方を作り込む
- [ ] 家族に触ってもらう

### Phase B: 本物に繋ぐ
- [ ] **上限付きの API キーを用意する**（Anthropic の Workspace ごとの月次上限）
- [ ] 鍵を OS の鍵束に入れ、`.env` は参照だけにする
- [x] `AICHAT_MODE=live` で実際に答えさせる（Gemini `gemini-flash-latest`）
      - 詰まった点: 既定の思考レベル「最小」をこのモデルが拒否する。「低」で通る
      - 400 のときの画面表示を「設定を確認してください」に直した
- [ ] 応答遅延を測る（話し終わり → 喋り始め 2 秒以内か）
- [x] 音声認識の比較 → **macOS の SpeechAnalyzer（ohr）を採用**（0.14秒・誤り0件）。
      whisper turbo は 0.74秒・1件。**サーバーを Mac に据え置くと決めたので、
      これがそのまま本番の音声認識になる**

### Phase C: ウェイクワードを実マイクで詰める
- [x] 判定の仕組み（連続 ASR + 文字列一致）と試験画面
- [x] 合成音声で測る → 検出 10/10・誤起動 0/15
- [ ] **実マイクで測り直す。** 上の数字は合成音声のもので、楽観しすぎない
- [ ] 誤起動の頻度（テレビを付けたまま放置する）
- [ ] 検出率（離れた位置・小声・早口）
- [ ] **AEC の確認**（読み上げ中に自分の声で起動しないか）。
      いまは読み上げ中に判定を止めている。まずはこれで足りるはず
- [ ] 連続 ASR の CPU が長時間持つか（12〜15% が持続するか、熱でどうなるか）

### Phase D: サーバーを据え置きにする
- [ ] `web/dist` をビルドし、Vite を止めてサーバー1つで配る
- [ ] **Mac を寝かせない**（`caffeinate -dims`）。寝ると端末が黙る
- [ ] ログイン時に全部立ち上がる状態にする（ohr は LaunchAgent 済み、VOICEVOX は Docker）
- [ ] 落ちたときの復帰を確かめる

### Phase E: 端末を作る
- [ ] ハードを選ぶ（[06](06-device-implementation.md): Pi Zero 2 W / ESP32-S3 / Pi 4）
- [ ] 部品購入・組み立て
- [ ] 端末側（150 行程度）を書く。マイクを送る・音を鳴らす・文字を描くだけ
- [ ] **`HOST` を LAN に開く。あわせて `/api/*` と `/ws` に認証を足す**
- [ ] **音声を流す経路を保護する**（TLS か共有鍵）。部屋の音が常時 LAN を流れる
- [ ] ケース・スタンド

### Phase F: 仕上げ
- [ ] カメラで撮って質問
- [ ] 物理ミュート
- [ ] `deploy.sh`

## 未決事項

| 項目 | 選択肢 | 決め方 |
|---|---|---|
| ウェイクワードの語 | いまは「ずんだもん」/「すんだもん」 | 合成音声では成立した。**実マイクで測り直して決める** |
| マイク | USB 会議マイク vs ReSpeaker HAT | まず手持ちの USB 会議マイクで試す |
| VOICEVOX の声 | 話者 ID | Phase A で家族に聞き比べてもらう |
| 端末のハード | Pi Zero 2 W / ESP32-S3 / Pi 4 | [06](06-device-implementation.md)。描画の速さと作業量で決める |
| ストリームの保護 | TLS か共有鍵か | LAN に開く前に決める |
| 追い質問の窓 | いまは 8 秒 | 短いと不便、長いと部屋の話し声を拾う。使って調整 |
| 思考レベル | いま「低」。モデルによって使える段が違う | 応答の速さと質を見て調整 |
| 無音検出 | RMS のまま / Silero VAD | 実際の部屋で外れるようなら差し替え |
| 遠隔からの設定変更 | SSH ポート転送のまま / 他の手段 | 面倒なようなら考え直す |

## 変更の記録

- **iPad で先に検証する案は不採用**（[02](02-device-selection.md) の C）。
  常時待ち受けが iOS の制約でできず、書いた Swift が流用できないため。
  代わりに Mac のブラウザで確かめる形にした。**結果としてこの判断が効いた** —
  サーバーを Mac に据え置くと決めたいま、そのとき書いたものはほぼ本番のまま残っている。
- **「Mac で確かめて Pi 5 に移す」構成をやめた**（[06](06-device-implementation.md)）。
  音声を常時サーバーへ流す形にした結果、**ウェイクワードの判定もサーバーでできる**
  ことが分かり、端末に機械学習を載せる理由が消えた。そこで
  **サーバーは Mac に据え置き、端末は「マイクを送る・音を鳴らす・文字を描く」だけ**にする。
  - 得た: macOS の音声認識（0.14秒・誤り0件）が**本番でもそのまま使える**。
    whisper への切り替え、VOICEVOX の実時間比、メモリ — [05](05-issues.md) の C 章の
    リスクが**まとめて消えた**。端末は安く小さくできる（Pi Zero 2 W / ESP32-S3）
  - 失った: Mac を常時起動させておく必要がある。**部屋の音が常時 LAN を流れる**ので、
    経路の保護と `/api/*` の認証が要る（それまでは `HOST` を開かない）
  - 伴って **Pi 5 + Touch Display 2 は不採用**（[02](02-device-selection.md)）。
    推論が載らないので、あの性能も画面の大きさも要らなくなった
- **Cloudflare Worker を廃止し、すべてローカルサーバーに寄せた。**
  Worker の存在理由は「ブラウザに鍵を置けないから中継が要る」だったが、
  ローカルサーバー自身が鍵を持てるので二重に置く必要が無かった。
  代償（家に置く箱に課金キーが載る／`/admin` に外から入れない）は、
  上限付きキー・OS の鍵束・SSH ポート転送で受け止める。
